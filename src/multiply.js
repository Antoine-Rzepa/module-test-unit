export function multiply(a, b){
    //GREEN 2
    if(isNaN(a)){
        return a + " is not a number" 
    }else if (isNaN(b)){
        return b + " is not a number"
    }else{
        //GREEN 1
        return a * b
    }
}